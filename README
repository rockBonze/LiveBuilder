* To build production environment
  * sudo apt-get install binfmt-support qemu-user-static live-build gcc-10-aarch64-linux-gnu make
  * sudo dpkg -i ./tools/*
  * sudo apt-get install -f

* To build ubuntu rootfs:
  * run make clean
  * run make config
  * run make

* To chroot ubuntu binary:
  * run make chroot
  * run make unchroot
  
* To build ubuntu image:
  * run make ubuntu

